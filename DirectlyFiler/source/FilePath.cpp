#pragma once
#include <iostream>
#include <fstream>
#include <string>
#include <filesystem>
#include "FilePath.h"

void FilepathLoad()
{

    std::string filepath;
    std::string path;
    std::ifstream path;
    std::cin >> path;
    std::cout << path << std::endl;
    if (!filepath)
    {
        std::ifstream filepath.open(path, std::ios::out);

    }
    else
    {
        std::ifstream filepath.open(path, std::ios::out);
        std::cin.get();
    }
}

void Folderreset()
{
    char filepath[256];
    std::fstream filepath;
    if (!filepath)
    {
        filepath == NULL;
        abort();
    }
    else
    {
        abort();
    }
}